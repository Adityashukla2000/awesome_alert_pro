/// Defines the type of alert to display.
///
/// Used to control icon, color, and UI styling.
enum AlertType {
  /// Success alert (green UI)
  success,

  /// Error alert (red UI)
  error,

  /// Info alert (blue UI)
  info,
}
